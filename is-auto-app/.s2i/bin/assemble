#!/bin/bash

echo "Starting assemble..."

cd /tmp/src

echo "Installing dependencies into application directory..."

if [ -f requirements.txt ]; then
    pip install --no-cache-dir -r requirements.txt --prefix=/opt/app-root/src
else
    echo "requirements.txt not found"
    exit 1
fi

echo "Assemble completed successfully"